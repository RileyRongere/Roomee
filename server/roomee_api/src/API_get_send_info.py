# Authors:

# for api use to create and query the py objects(Make sure to get the id as well). Should return json objects. Should take in id? or username?

# not sure if this file will be needed or if API team should just use what is currently labeled 'insert_query_DP.py'
# May just contain wrapper functions that call the functions of the other file.
